# Requirements file for backward compatibility
#
# NOTE: This file is kept for backward compatibility. The authoritative source
# of dependencies is pyproject.toml. For optional dependencies, use:
#
#   pip install jarvis-ai[voice]        # Full voice support
#   pip install jarvis-ai[voice-input] # Voice input only (STT)
#   pip install jarvis-ai[voice-output] # Voice output only (TTS)
#   pip install jarvis-ai[dev]         # Development tools
#   pip install jarvis-ai[docs]        # Documentation tools
#   pip install jarvis-ai[all]         # Everything
#
# Minimal install (CLI/Text only):
#   pip install jarvis-ai
#
# This file contains all dependencies for a full installation including voice features.
# For minimal install, use: pip install jarvis-ai
# For voice features only: pip install jarvis-ai[voice]

# Core dependencies (always installed)
ollama>=0.5.3
python-dotenv>=1.1.1
mcp[cli]>=1.13.1
httpx>=0.28.1
pathspec>=0.12.1

# Voice input dependencies (optional - install with jarvis-ai[voice-input])
vosk>=0.3.45
sounddevice>=0.5.2
numpy>=2.2.0

# Voice output dependencies (optional - install with jarvis-ai[voice-output])
# NOTE: piper-tts and onnxruntime removed for Python 3.14 compatibility
# These will be re-added when Python 3.14 wheels become available
